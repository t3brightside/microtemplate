# Removes third template from Template drop down in content element settings
 TCEFORM.tt_content.tx_pagelist_template.removeItems = 2

 # Adds new template to content element Template drop down
 TCEFORM.tt_content.tx_pagelist_template.addItems {
		3 = Articles, no img (microtemplate)
 }
